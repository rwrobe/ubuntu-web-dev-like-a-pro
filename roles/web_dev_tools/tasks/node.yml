---

- name: Install NVM and Node Latest
  shell: |
    axel -n {{ downloadThreads }} "https://raw.githubusercontent.com/creationix/nvm/v0.33.11/install.sh" chdir={{ downloadDir }} creates=install.sh | bash install.sh
    echo 'export NVM_DIR="$HOME/.nvm"
      [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
      [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion ' >> ~/.zshrc

- name: Install node latest
  shell: nvm install stable

- name: Install Task Runners
  shell: "npm install -g {{ item }}"
  with_items:
    - grunt-cli
    - gulp
    - webpack

- name: Install Yarn
  shell: |
    axel -n {{ downloadThreads }} https://dl.yarnpkg.com/debian/pubkey.gpg chdir={{ downloadDir }} creates=pubkey.gpg | sudo apt-key add -
    echo "deb https://dl.yarnpkg.com/debian/ stable main" | sudo tee /etc/apt/sources.list.d/yarn.list
    apt-get update
    apt-get install --no-install-recommends yarn
  become: yes